Show that boosting with linear models as baselearner and $L2$ loss is equivalent to fitting a linear model via gradient descent.

\textbf{Hint:} Note that adding a linear base learner to an linear model is equivalent to summing parameters.
