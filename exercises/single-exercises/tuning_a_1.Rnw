Tune a SVM for the Ionosphere dataset in parallel with random search. Use nested resampling for a clean evaluation (\textbf{hint:} This is easy with the \texttt{TuneWrapper}). Extract the selected SVM-kernels with their parameters and inspect them.
