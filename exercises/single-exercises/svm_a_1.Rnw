\begin{itemize}
\item Show: If $k_1, k_2$ are kernels and $\alpha, \beta \geq 0$, then $k(x, z) := \alpha k_1(x, z) + \beta k_2(x, z)$ is also a kernel.

\item Show, that for every kernel $k$:
\begin{enumerate}
\item[(i)] $k(x,x) \geq 0 \quad \forall x \in \mathcal{X}$ and 
\item[(ii)] if $k(x,x) = 0 \quad \forall x \in \mathcal{X}$, then $k$ is the nullfunction.
\end{enumerate}
\end{itemize}