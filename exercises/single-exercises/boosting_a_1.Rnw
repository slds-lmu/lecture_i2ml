Implement you own simple version of \texttt{AdaBoost} in R. Use \texttt{rpart} to construct your base learner (\textbf{hint:} \texttt{rpart} has a build in weights option). Compare your results with the \texttt{ada} function from the package \texttt{ada}.
