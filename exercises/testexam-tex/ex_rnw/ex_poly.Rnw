State the hypothesis space for a univariate polynomial regression model with
\begin{enumerate}
  \item degree $d = 4$.
  \item Extend the model with additional first-order interactions between all terms of even order.
\end{enumerate}