State the hypothesis space for a univariate polynomial regression model with
\begin{itemize}
  \item degree $d = 4$,
  \item factors equal to the inverse degree for each term (except for the 
  intercept), and
  \item first-order interactions between all terms of even order.
\end{itemize}